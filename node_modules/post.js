var http = require('http');
http.post = require('http-post');

var args=[[0, 1, 2], [3, 4, 5], [1, 2, 3], [6, 6, 5]];

var kwargs = {"filename": "plot from node api",
        "fileopt": "overwrite",
        "style": {
            "type": "bar"
        },
    "traces": [1],
    "layout": {
        "title": "experimental node data"
    },
    "world_readable": true
};



var un= "demos";
var key= "tj6mr52zgp";
var origin="plot";
var platform="node";

function plotly(args, un, key, origin, kwargs) {
	args = JSON.parse(args);
	kwargs = JSON.parse(kwargs);
	payload = {'platform': platform, 'version': __version__, 'args': args, 'un': un, 'key': key, 'origin': origin, 'kwargs': kwargs};

	http.post('https://plot.ly/clientresp', data , function(res){
    response.setEncoding('utf8');
    res.on('data', function(chunk) {
        console.log(chunk);
    });
});

}





